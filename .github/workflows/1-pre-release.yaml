name: Validate PR

on:
  push:
    branches:
      - feature**
      - bugfix**
      - hotfix**

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        uses: ./.github/workflows/test.yaml
